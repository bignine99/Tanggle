# 탱글성형외과 YouTube 콘텐츠 - 자막 추출 전용 (완전 무료)

# ── 필수 (무료) ───────────────────────────────────────────────
yt-dlp>=2024.1.1                  # 채널 크롤링 + 오디오 추출
youtube-transcript-api>=0.6.2     # YouTube 자막 무료 추출

# ── 로컬 Whisper (자막 없는 영상용, 완전 무료) ───────────────
faster-whisper>=1.0.0             # 로컬 STT 엔진 (CPU/GPU 모두 지원)

# ── 유틸 ─────────────────────────────────────────────────────
tqdm>=4.66.0                      # 진행률 표시
pandas>=2.2.0                     # 결과 집계

# ── 나중에 필요시 (Phase 2~3 홈페이지/챗봇) ──────────────────
# openai>=1.12.0                  # 구조화/RAG 단계에서만 사용
# chromadb>=0.4.24
# python-dotenv>=1.0.0
